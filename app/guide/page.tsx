'use client';

/**
 * /guide — «Руководство»: подробный гид по приложению.
 *
 * Раньше страница была жёстко зашитым списком из восьми строк вида
 * «эмодзи + название + одна фраза». Это оглавление, а не руководство:
 * человек не узнавал ни что делать, ни в каком порядке.
 *
 * Теперь содержимое живёт в таблице articles (раздел 'guide') и
 * пишется из админки, как «Сира» и «Нохчалла». Ровно поэтому глава,
 * добавленная через редактор, здесь и не появлялась — страница её не
 * читала.
 */

import { BookOpen } from 'lucide-react';
import ReadingPage from '@/components/reading/ReadingPage';

export default function GuidePage() {
  return (
    <ReadingPage
      section="guide"
      icon={BookOpen}
      title="Руководство"
      titleCe="Хьехам"
      subtitle="Как пользоваться приложением"
      subtitleCe="Гlирс муха лело"
      emptyHint="Главы руководства готовятся к публикации."
      emptyHintCe="Хьехаман дийцарш зорбане кечдеш ду."
    />
  );
}
