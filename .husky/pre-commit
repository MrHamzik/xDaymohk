# Проверяем только то, что реально уходит в коммит: CSS через stylelint,
# TypeScript через eslint. Полная сборка здесь была бы честнее, но она
# идёт ~50 секунд на каждый коммит — это заставит людей ставить --no-verify.
# Синтаксис CSS ловится и так: именно на нём проект однажды слёг.
npx lint-staged
