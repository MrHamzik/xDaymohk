/**
 * Метка сборки — для диагностики «какой код сейчас работает».
 *
 * Повод: у владельца сайт упорно вёл себя как старая версия, хотя git
 * показывал последний коммит. Разбор Hydration-ошибки показал, что
 * сервер и браузер рендерили РАЗНЫЕ версии одного компонента — на
 * машине работала сборка, которой нет в репозитории (локальные правки
 * и/или закэшированные чанки .next, и/или второй dev-сервер).
 *
 * Как пользоваться:
 *   1. Открыть исходник страницы (Ctrl+U) и найти «daymohk-build» —
 *      это рендер СЕРВЕРА. Нет метки — :3000 обслуживает другой код.
 *   2. В консоли браузера: «[daymohk] сборка: …» — это клиентский
 *      бандл. Нет строки — браузер держит старые файлы (сервис-воркер).
 *
 * Метку меняем при каждом дальнем коммите, чтобы проверка была честной.
 */
export const BUILD_MARK = 'daymohk-2026-08-22-14';
